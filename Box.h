class Box {
    private:
       int length;
       int width;
       int height;
    public:
       // write prototypes of setters for length, width and height
      void setValues(int slength,int swidth,int sheight);
       // write prototypes of getters for length, width and height
      
      void get
       int calcVolume();
};
